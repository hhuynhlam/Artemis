extends ../_layout.jade

block content
    section#Event
        ol.breadcrumb
            li
                a(href="#/") Home
            li.active Events

        .container
            h2 
                | Upcoming Events
                .btn.btn-primary.pull-right(data-bind="visible: eventView() === 'upcoming', click: seePastEvents") View Past Events
                .btn.btn-primary.pull-right(data-bind="visible: eventView() === 'past', click: seeUpcomingEvents") View Upcoming Events
            
            .well.col-xs-12

                #FilterEvents(data-placeholder="Filter Events")

                table.table.col-xs-12
                    thead
                        tr
                            th Date
                            th Name
                    tbody(data-bind="foreach: formattedEvents")
                        tr
                            td(data-bind="text: date")
                            td
                                a(data-bind="attr: { href: window.env.CLIENT_HOST + '/event/' + id }")
                                    span(data-bind="text: name")

                div.text-center
                    button.btn.btn-primary(data-bind="visible: isMore, click: seeMore") See More
                    button.btn.btn-primary(data-bind="visible: !isMore()" disabled) No More