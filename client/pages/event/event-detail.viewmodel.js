'use strict';

define(function (require) {
    var ko = require('knockout');
    var sandbox = require('sandbox');

    var EventDetailViewModel = function (eventId) {
        debugger;
    };

    EventDetailViewModel.prototype.getEvents = function (options) {

    };

    return EventDetailViewModel;
});